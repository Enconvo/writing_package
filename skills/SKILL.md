---
name: writing_package
description: >
  AI-powered writing toolkit with commands for grammar/spelling correction, text editing, rewriting, tone adjustment, summarization, proofreading, key point extraction, format conversion, and dictation polishing via configurable LLM providers.
metadata:
  author: EnconvoAI
  version: "0.0.305"
---

## API Reference

Just use the `local_api` tool to request these APIs.

| Endpoint | Description |
|----------|-------------|
| `writing_package/emoji` | Give An Emoji Suggestion for given text. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/explain` | Explain the text. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/screenshot_explain` | Explain the selected area. _No params_ |
| `writing_package/summarize` | Summarize the text. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/fix_spelling_and_grammar` | Fix Spelling and Grammar. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/ai_edit` | AI Edit. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/improve_writing` | Improve Writing. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/polish` | Improve Writing. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/make_longer` | expand the text to make it longer. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/make_shorter` | Make Shorter. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/change_tone_to_friendly` | Change Tone to Friendly. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/change_tone_to_confident` | Change Tone to Confident. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/change_tone_to_casual` | Change Tone to Casual. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/proofread` | Proofread the text. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/rewrite` | Rewrite the text. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/change_tone_to_professional` | Change Tone to Professional. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/make_concise` | Make the text more concise. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/extract_key_points` | Extract key points from the text. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/convert_to_list` | Convert the text to a list format. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/convert_to_table` | Convert the text to a table format. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/dictation_polish` | Clean up voice input text by removing filler words and fixing typos. _4 params — use `check_local_api_schemas` tool_ |
| `writing_package/dictation_translate` | Translate voice input text into the target language. _4 params — use `check_local_api_schemas` tool_ |

